# ==================================
# BINANCE API CONFIGURATION
# ==================================
# Get your testnet keys from: https://testnet.binance.vision/
BINANCE_API_KEY=your_testnet_api_key_here
BINANCE_API_SECRET=your_testnet_api_secret_here

# Use testnet for development, mainnet for production
BINANCE_ENV=testnet  # testnet or mainnet

# Symbols to collect data for (comma-separated, no spaces)
BINANCE_SYMBOLS=BTCUSDT,ETHUSDT,BNBUSDT

# Timeframes/Intervals to collect (comma-separated, no spaces)
# Options: 1m, 3m, 5m, 15m, 30m, 1h, 2h, 4h, 6h, 8h, 12h, 1d, 3d, 1w, 1M
BINANCE_INTERVALS=1m,5m,15m,1h

# ==================================
# DATABASE CONFIGURATION
# ==================================
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=crypto_ai
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres

# Full connection string (auto-generated if not provided)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/crypto_ai

# Enable TimescaleDB features
TIMESCALE_ENABLED=true

# ==================================
# APPLICATION SETTINGS
# ==================================
# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# Batch size for bulk database inserts
BATCH_SIZE=100

# WebSocket reconnection delay in seconds
RECONNECT_DELAY=5

# Maximum retries for failed operations
MAX_RETRIES=3

# Health check interval in seconds
HEALTH_CHECK_INTERVAL=60
